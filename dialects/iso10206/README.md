# ISO 10206 Extended Pascal

Extended Pascal compatibility boundary.

The ISO 7185 baseline is retained conceptually, while Extended Pascal facilities are tested separately.

Extended features are never silently downgraded into unrelated Delphi or ObjFPC semantics.
